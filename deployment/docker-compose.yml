services:
  tennis-analytics:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    volumes:
      - ../data:/app/data
      - ../reports:/app/reports
      - ../logs:/app/logs
      - ../models:/app/models
    restart: "no"
